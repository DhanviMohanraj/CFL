"""DriftAdapt Application Root Package.

Author: DriftAdapt Contributors
Purpose: Serves as the primary namespace and entrypoint container for the DriftAdapt system.
Future Integration: Coordinates sub-packages (api, core, federation, models, drift, etc.)
                    and handles high-level module bootstrapping.
"""

__version__ = "0.1.0"
